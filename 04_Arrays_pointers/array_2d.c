#include <stdio.h>
#include <conio.h>

int main ()
{
    int arr [3][3];
    int num ;
    // value  using for loop
    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; j++)
        {
            printf("Enter the whole array");
            scanf("%d", &arr[i][j]);
            
        }
        
    }
    
}