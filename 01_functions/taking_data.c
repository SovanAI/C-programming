#include<stdio.h>
int main(){
    int a, b;
    printf("Give the value of a and b: ");
    scanf("%d%d",&a,&b);
    printf("The values are %d and %d",a,b);
    int sum = a +b ;
    printf("Sum is : %d" , sum );
    return 0;
}
